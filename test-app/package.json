{
  "name": "gc-letters-test-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:package": "npm run test:package:build && npm run test:package:install",
    "test:package:build": "cd .. && npm pack",
    "test:package:install": "npm install ../gc-letters-1.0.0.tgz && echo 'Package installed! Now run: npm run dev'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "@vitejs/plugin-react": "^4.2.1",
    "canvas": "^3.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
